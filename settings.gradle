if(VERSION_NAME.endsWith("-SNAPSHOT")){
    include ':analysis-sample', ':module-library'
    include ':kotlin-sample'
}
// Sets the output JAR filename
rootProject.name = 'library-analysis-gradle-plugin'
include 'plugin'